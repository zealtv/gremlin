# s01-layout

Create `.scribe/` with `bin/`, `tools/`, `skills/`, `transcript-archive/`. Touch empty `transcript.md`. Write a one-paragraph stub `tend.md` describing the gremlin's identity ("you are the scribe..." is fine for now).

**Verify:** `ls .scribe/` shows the canonical top-level entries from the README's layout section.
