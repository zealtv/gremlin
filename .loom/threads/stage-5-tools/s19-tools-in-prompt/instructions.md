# s19-tools-in-prompt

`tend-loop.sh` includes `tools/README.md` in the prompt assembly. Order is: `tend.md`, `tools/README.md`, `transcript.md`, item body. (Skills slot in at stage 6.)

**Verify:** Capture a prompt to a file (temporary debug) and confirm the tools list is present.
