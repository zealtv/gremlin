# stage-3-local-cli

**Outcome.** `./.scribe/say "hello"` writes a message into `.nest/in/`, blocks until a reply lands in `.nest/out/`, prints the reply. End-to-end conversation without launching a daemon.

Tie this stitch when every child is tied and a short note below records what was learned.

## Notes
