# s11-end-to-end

Run `bin/tend-loop.sh` in a loop in one terminal (`while true; do bin/tend-loop.sh; sleep 5; done`). In another, run `./.gremlin/say "hi"`. The reply should arrive and print.

**Verify:** Two-turn conversation completes through `say` alone.
