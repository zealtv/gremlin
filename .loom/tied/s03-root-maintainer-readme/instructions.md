# s03-root-maintainer-readme

Rewrite the root README as the GitHub and maintainer entry point.

Acceptance:

- Install one-liner is near the top.
- Quick start uses `run.sh` plus the TUI bridge as the primary interaction.
- `bin/say` is documented as one-shot/scripting.
- Maintainer workflow is summarized in root README.
- Duplicate loom details and "Next items" are removed.
- `DEVELOPING.md` is deleted after useful content is merged.
