# s12-acceptance-3

Three-turn conversation through `say`. Verify:

- Each reply prints within a few seconds.
- The transcript shows six turns in order.
- `.nest/in/` and `.nest/out/` are empty (everything in `out/sent/`).
