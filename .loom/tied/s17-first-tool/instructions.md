# s17-first-tool

`tools/now.sh`: prints the current time in ISO 8601, exits 0. Tiny, deterministic, easy to spot in a transcript. Add it to `tools/README.md`.

**Verify:** `./tools/now.sh` prints a timestamp.
