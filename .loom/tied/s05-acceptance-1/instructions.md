# s05-acceptance-1

Walk the layout and confirm:

- Every protocol's invariants hold (`.nest/` and `.groundhog/` look like the family READMEs say they should).
- Every shell script under `bin/` is `bash -n` clean.
- `bin/llm.sh` returns a reply for a trivial prompt.
