# s05a-root-readme

Update only the root `README.md` for the new prompt-build model.

Keep the changes surgical:

- Soften the "Memory you control" bullet so it says the memory catalog is
  broadcast by default, finding bodies are fetched on demand, and selected
  findings can be promoted to full broadcast by symlinking into `context/`.
- Update the directory tree to show `context/system/`.
- Do not edit `.gremlin/README.md`, protocol docs, composition docs, or
  `context/system/README.md` in this stitch.
