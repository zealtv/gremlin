# deep — opus, for harder reasoning
MODEL=claude-opus-4-7
EXTRA_FLAGS=""
# Add any future thinking / reasoning-budget flags here as the CLI exposes
# them. Each preset is whatever the wired-in CLI accepts.
