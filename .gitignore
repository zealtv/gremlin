.DS_Store
.gremlin/run.log
.gremlin/bridges/*/config
.gremlin/bridges/*/*.pid
.gremlin/bridges/*/*.log
.gremlin/bridges/*/.cursor
.gremlin/bridges/*/.cache/
.gremlin/bridges/*/.update-offset
__pycache__/
 
.claude/
# maintainer's dev loom only — anchored so it does NOT catch the shipped
# .gremlin/.loom/ (a deployed gremlin's own loom, which is tracked canonical).
/.loom/
council/
